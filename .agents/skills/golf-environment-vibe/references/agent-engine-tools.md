# Agent engine tools

Use the canonical workflow at `../../golf-course-authoring/references/agent-engine-tools.md`.

Environment work must inspect the course and affected regions, query the licensed catalog, stage semantic commands through the actual builder, run analyzers when protected placement may change, and review the staged transaction before output. A text summary is not authoritative terrain data.
